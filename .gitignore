node_modules
dist
dist-ssr
*.local
.DS_Store

# Secrets
.env
.env.*
!.env.example

# Source diligence documents — kept out of git for now.
# The pipeline reads these at runtime; see docs/superpowers/specs for the
# expected filenames. Scoring tests do not need them (the workbook values are
# transcribed into shared/src/scoring.fixture.ts).
data/

# Python virtualenv
.venv/
venv/
__pycache__/
*.pyc

# Backend runtime artifacts (share registry + generated memos)
agent_backend/shares.json
agent_backend/memos/

# Grid data products — regenerated by scripts/grid/ (fetch_grid_data.py + build_tiles.sh);
# NOT committed per grid-v1 contract section 1.
agent_backend/data/grid/
